const Home = ({ styleToAdd }) =>{
    return(
        <div style={styleToAdd}>
            <header className="App-header">
                <h1>Airline Manager</h1>
            </header>
        </div>
    );
};

export default Home;